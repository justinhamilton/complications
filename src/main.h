#pragma once
  
//BitmapLayer *logo_layer;
int battery;

int show_second_hand;

bool connected_bool;

GColor background_color;
GColor accent_color;
GColor hand_color;
GColor face_color;
GColor bold_tick_color;

GFont logo_font;

int show_minute_ticks;
int show_hour_ticks;

bool charging;